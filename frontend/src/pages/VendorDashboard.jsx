import React from 'react'

export default function VendorDashboard() {
  return (
    <div className="container mx-auto">
      <h2 className="text-2xl font-bold">Vendor Dashboard</h2>
      <p className="mt-2 text-gray-600">Add/Edit Product form, Inventory table, and metrics cards to be implemented by Member 4.</p>
      <div className="mt-4 border rounded p-4 bg-white">
        <h3 className="font-semibold">Quick Add (Image URL only)</h3>
        <p className="text-sm text-gray-500">Form skeleton ready — implement state + API calls to /api/products (vendor)</p>
      </div>
    </div>
  )
}
